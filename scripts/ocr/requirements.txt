# Python 3.11 ONLY — PaddlePaddle is incompatible with Python 3.13
# Install steps:
#   pip install paddleocr==3.3.0
#   pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
#   pip install pymupdf pillow

paddleocr==3.3.0
pymupdf>=1.27
Pillow>=10.0.0
# paddlepaddle==3.2.0 must be installed from PaddlePaddle mirror (see above)
